export interface Constants {
  darkestColor: string;
  darkColor: string;
  primaryColor: string;
  lightColor: string;
  lightestColor: string;
  minWidthPc: number;
  maxWidthPhone: number;
  minWidthPhone: number;
}